#ifndef _TESTER_
#define _TESTER_
#include <iostream>


/**
	This function reads a series of HTTP requests stored in the file name provided as arguments.
	Then makes each request individually, with different connections.
*/
void ProcessFile(std::string);

#endif
